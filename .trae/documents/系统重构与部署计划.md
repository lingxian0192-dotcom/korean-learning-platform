# 重构中英文双语系统与 API 面板集成部署计划

我将分三个阶段为您完成此次升级：

## 阶段一：管理员后台双语重构 (Refactor Bilingual System)
目前管理员后台 (`AdminDashboard`) 为全英文硬编码，我将对其进行完全的国际化改造。
1.  **提取文案**: 扫描后台页面所有硬编码文本（如 "Resource Management", "Add Resource" 等）。
2.  **配置语言包**:
    *   更新 `apps/client/src/i18n/locales/en/translation.json`
    *   更新 `apps/client/src/i18n/locales/zh/translation.json`
3.  **代码替换**: 使用 `t()` 函数替换界面文本，确保后台能随系统语言切换中英文。

## 阶段二：API Key 管理面板集成 (API Key Integration)
为了让您在后台直接填写 API Key，我需要打通前后端存储。
1.  **数据库准备**: 创建 `system_settings` 表的 SQL 脚本，用于存储系统配置。
2.  **后端开发**:
    *   创建 `SettingsModule`，提供读写 API Key 的接口 (`GET/POST /api/v1/settings/api-key`)。
    *   升级 `AiService`，使其优先从数据库读取 API Key，如果数据库为空则回退使用环境变量。
3.  **前端开发**:
    *   在管理员面板增加「系统设置 (Settings)」 标签页。
    *   添加 API Key 输入框与保存按钮，对接后端接口。

## 阶段三：VPS 一键部署 (Deployment)
针对您提供的服务器信息，我将提供一键部署方案。
1.  **创建部署脚本**: 编写 `deploy/deploy_vps.ps1` (PowerShell)，自动化以下流程：
    *   **本地构建**: 编译前端 (`dist`) 和后端 (`bundle`)。
    *   **文件传输**: 将构建产物压缩并通过 `scp` 上传至服务器 `115.191.23.62`。
    *   **远程执行**: 通过 `ssh` 自动重启 Docker 服务。
2.  **执行部署**:
    *   脚本将设计为交互式，运行时会提示您输入密码 (`7Fp#kT9x!L2s$V@NqY8z^C&H*dM4bW`)。
    *   由于安全限制，我无法在后台静默输入密码，因此需要您在终端运行此脚本（将提供详细命令）。

## 准备工作
- [x] 确认项目结构
- [x] 确认 VPS 凭证可用性
- [ ] 等待您的确认开始编码
